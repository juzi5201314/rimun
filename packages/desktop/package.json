{
  "name": "@rimun/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "electrobun dev",
    "dev:cdp": "bash ./scripts/with-cef-automation-env.sh dev",
    "dev:host": "bun run src/bun/dev-host.ts",
    "dev:watch": "electrobun dev --watch",
    "dev:watch:cdp": "bash ./scripts/with-cef-automation-env.sh dev --watch",
    "run": "electrobun run",
    "build": "electrobun build --env=stable",
    "build:dev": "electrobun build --env=dev",
    "build:test:cdp": "bash ./scripts/with-cef-automation-env.sh build --env=test",
    "test": "bun test",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@rimun/domain": "workspace:*",
    "@rimun/shared": "workspace:*",
    "drizzle-orm": "^0.44.5",
    "electrobun": "1.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
